## Decision 1: Document as a hedged "counter-case," not a revision of Fingerprint D

The guide's existing Fingerprint D framing ("+15ms to +90ms+ Delta") remains valid as the *typical* pattern observed across this guide's larger captures (e.g. Trace 3d/3f). The new observation is a real, live, reproducible counter-example on the same day as a separate ICMP-throttling incident, but it is based on only n=6 (direct) vs n=9 (tunneled) ad-hoc manual pings — far below this guide's own ~120-sample bar for quantitative claims (Decision 16 in the archived `wifi-latency-and-enterprise-forensics-guide` change). Rather than revise the headline Fingerprint D numbers, add a clearly-labeled "counter-case" callout stating: (a) `OVH` can legitimately be negative, (b) this was observed live and is consistent with Zscaler's own advertised "faster via better peering" claim, (c) the specific numbers cited are anecdotal (small n) and may also be partly explained by residual jitter on the direct path recovering from the same-session ICMP-throttling incident, not purely a peering-quality effect.

## Decision 2: Do not create a new Trace entry / commit a raw log for this

This observation came from ad-hoc terminal `ping` commands (pasted directly by the user), not a `ping_checker.py --logfile` capture. Per the guide's "Raw Trace Evidence Is Committed to the Repository" requirement, only `ping_checker.py`-captured traces get a numbered `### Trace X` entry with a committed raw log. This finding is documented as a narrative callout with the manual ping output inlined (a handful of lines, already fully visible in the chat/PR context), not as a new Trace — a future contributor wanting to properly quantify this should run a real `--count 120` capture and add a proper Trace entry then, per the existing sample-size and raw-log requirements already in the spec.
